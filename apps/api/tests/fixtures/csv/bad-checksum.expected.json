{
  "_comment": "national_id has an invalid Israeli-ID checksum digit. workerSchema's Zod refine() rejects it, surfacing as a per-row error (WorkforceImportService.importCsv never throws for this -- the row is just recorded as failed and the batch continues).",
  "kind": "workforce",
  "month": "2027-02",
  "call": "WorkforceImportService.importCsv(csvText, month, companyId)",
  "expectImportResult": {
    "totalRows": 1,
    "inserted": 0,
    "updated": 0,
    "failed": 1,
    "failedRows": [
      { "row": 1, "field": "nationalId", "messageContains": "checksum" }
    ]
  }
}
